# Conexion local con Docker Compose + Postgres + Adminer
DATABASE_URL="postgresql://saludplena:saludplena@localhost:5432/saludplena_demo?schema=public"

# En produccion (Vercel + Neon / Supabase / Vercel Postgres) reemplazar por la URL real
# DATABASE_URL="postgresql://USER:PASS@HOST:PORT/DB?sslmode=require"
